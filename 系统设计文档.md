# 学生成绩管理系统 - 系统设计文档

## 📋 目录

1. [项目概述](#1-项目概述)
2. [系统架构](#2-系统架构)
3. [技术栈](#3-技术栈)
4. [数据库设计](#4-数据库设计)
5. [模块设计](#5-模块设计)
6. [API接口设计](#6-api接口设计)
7. [前端设计](#7-前端设计)
8. [部署说明](#8-部署说明)
9. [开发规范](#9-开发规范)

---

## 1. 项目概述

### 1.1 项目简介

学生成绩管理系统是一个基于Spring Boot开发的Web应用系统，主要用于管理学生选课、课程管理和成绩录入等功能。系统采用前后端分离架构，后端提供RESTful API接口，前端通过HTML页面与后端交互。

### 1.2 主要功能

- **用户管理**：用户注册、登录、删除
- **课程管理**：课程的增删改查
- **选课管理**：学生选课、退课
- **成绩管理**：成绩录入、查询

### 1.3 用户角色

- **学生（student）**：可以选课、查询成绩
- **教师（teacher）**：可以录入成绩、管理课程
- **管理员（admin）**：可以管理用户、课程等所有功能

---

## 2. 系统架构

### 2.1 架构模式

系统采用经典的**三层架构**模式：

```
┌─────────────────────────────────────┐
│         前端层 (Frontend)            │
│    HTML + CSS + JavaScript          │
└──────────────┬──────────────────────┘
               │ HTTP/HTTPS
               │ RESTful API
┌──────────────▼──────────────────────┐
│      控制器层 (Controller)           │
│  UserAuthenticationController       │
│  CourseManagementController         │
│  SelectCourseController             │
│  ScoreManagementController          │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│       服务层 (Service)               │
│  UserService                        │
│  CourseService                      │
│  CourseSelectService                │
│  ScoreService                       │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│     数据访问层 (Repository)          │
│  UserRepository                     │
│  CourseRepository                   │
│  CourseSelectRepository             │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│       数据库层 (Database)            │
│         MySQL 8.0+                  │
└─────────────────────────────────────┘
```

### 2.2 包结构

```
src/main/java/
├── Controller/              # 控制器层
│   ├── UserAuthenticationController.java
│   ├── CourseManagementController.java
│   ├── SelectCourseController.java
│   └── ScoreManagementController.java
├── Service/                 # 服务层
│   ├── UserService.java
│   ├── CourseService.java
│   ├── CourseSelectService.java
│   └── ScoreService.java
├── Repository/              # 数据访问层
│   ├── UserRepository.java
│   ├── CourseRepository.java
│   └── CourseSelectRepository.java
├── Entity/                  # 实体类
│   ├── User.java
│   ├── Course.java
│   ├── CourseSelection.java
│   ├── Student.java
│   ├── Teacher.java
│   └── Grade.java
└── org/lut/studentgrademanagmentsystem/
    └── StudentGradeManagmentSystemApplication.java
```

---

## 3. 技术栈

### 3.1 后端技术

| 技术 | 版本 | 说明 |
|------|------|------|
| Java | 17 | 编程语言 |
| Spring Boot | 3.5.6 | 应用框架 |
| Spring Data JPA | 6.2.11 | 数据持久化框架 |
| Hibernate | 6.6.29 | ORM框架 |
| MySQL Connector | 9.4.0 | MySQL数据库驱动 |
| Lombok | - | 简化Java代码 |
| Maven | - | 项目构建工具 |

### 3.2 前端技术

| 技术 | 说明 |
|------|------|
| HTML5 | 页面结构 |
| CSS3 | 样式设计 |
| JavaScript | 交互逻辑 |
| Fetch API | HTTP请求 |

### 3.3 数据库

- **数据库类型**：MySQL 8.0+
- **字符集**：UTF-8
- **连接池**：HikariCP

---

## 4. 数据库设计

### 4.1 数据库ER图

```
┌─────────────┐         ┌──────────────┐         ┌─────────────┐
│    User     │         │   Course     │         │   Student   │
├─────────────┤         ├──────────────┤         ├─────────────┤
│ user_id (PK)│         │ course_id(PK)│         │ student_id  │
│ username    │         │ name         │         │ name        │
│ password    │         │ credit       │         │ studentNum  │
│ role        │         │ teacherId    │         └─────────────┘
└─────────────┘         │ userId       │
                        └──────┬───────┘
                               │
                    ┌──────────▼──────────┐
                    │  CourseSelection    │
                    ├────────────────────┤
                    │ id (PK)            │
                    │ student_id (FK)    │
                    │ course_id (FK)     │
                    │ score              │
                    └────────────────────┘
```

### 4.2 数据表设计

#### 4.2.1 user表（用户表）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| user_id | INT | PRIMARY KEY, AUTO_INCREMENT | 用户ID |
| username | VARCHAR | NOT NULL, UNIQUE | 用户名 |
| password | VARCHAR | NOT NULL | 密码 |
| role | VARCHAR | NOT NULL | 角色（student/teacher/admin） |

#### 4.2.2 course表（课程表）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| course_id | INT | PRIMARY KEY, AUTO_INCREMENT | 课程ID |
| name | VARCHAR | NOT NULL | 课程名称 |
| credit | DOUBLE | NOT NULL | 学分 |
| teacher_id | INT | NOT NULL | 教师ID |
| user_id | INT | NOT NULL | 用户ID |

#### 4.2.3 course_selection表（选课表）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INT | PRIMARY KEY, AUTO_INCREMENT | 选课记录ID |
| student_id | INT | NOT NULL | 学生ID |
| course_id | INT | NOT NULL | 课程ID |
| score | INT | DEFAULT -1 | 成绩（-1表示未录入） |

**索引设计**：
- 联合唯一索引：(student_id, course_id) - 防止重复选课

#### 4.2.4 student表（学生表）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| student_id | INT | PRIMARY KEY, AUTO_INCREMENT | 学生ID |
| student_number | VARCHAR | UNIQUE | 学号 |
| name | VARCHAR | NOT NULL | 姓名 |

**注意**：Teacher和Grade实体类已定义但暂未使用，为未来扩展预留。

---

## 5. 模块设计

### 5.1 用户认证模块

**功能**：用户注册、登录、删除

**核心类**：
- `UserAuthenticationController`：处理用户相关HTTP请求
- `UserService`：用户业务逻辑处理
- `UserRepository`：用户数据访问

**主要方法**：
- `registerUser()`：用户注册
- `login()`：用户登录验证
- `deleteUser()`：删除用户

### 5.2 课程管理模块

**功能**：课程的增删改查

**核心类**：
- `CourseManagementController`：课程管理接口
- `CourseService`：课程业务逻辑
- `CourseRepository`：课程数据访问

**主要方法**：
- `getAllCourses()`：获取所有课程
- `addCourse()`：添加课程
- `updateCourse()`：更新课程信息
- `deleteCourseByName()`：删除课程

### 5.3 选课管理模块

**功能**：学生选课、退课

**核心类**：
- `SelectCourseController`：选课接口
- `CourseSelectService`：选课业务逻辑
- `CourseSelectRepository`：选课数据访问

**主要方法**：
- `chooseOneCourse()`：学生选课
- `deleteOneChosenCourse()`：学生退课
- `getAverageScoreForStudent()`：计算学生平均分
- `getAverageScoreForCourse()`：计算课程平均分
- `getCreditScoreForStudent()`：计算学生学分绩

### 5.4 成绩管理模块

**功能**：成绩录入、查询

**核心类**：
- `ScoreManagementController`：成绩管理接口
- `ScoreService`：成绩业务逻辑

**主要方法**：
- `addScore()`：录入成绩
- `getScore()`：查询成绩

---

## 6. API接口设计

### 6.1 接口规范

- **协议**：HTTP/HTTPS
- **数据格式**：JSON
- **字符编码**：UTF-8
- **跨域支持**：已配置（`@CrossOrigin(origins = "*")`）

### 6.2 接口列表

#### 6.2.1 用户认证接口

**基础路径**：`/user`

| 接口 | 方法 | 路径 | 功能 |
|------|------|------|------|
| 用户注册 | POST | `/user/register` | 注册新用户 |
| 用户登录 | GET | `/user/login` | 用户登录 |
| 删除用户 | DELETE | `/user/delete/{username}` | 删除用户 |

**请求示例**：
```json
POST /user/register
Content-Type: application/json

{
  "username": "zhangsan",
  "password": "123456",
  "role": "student"
}
```

**响应示例**：
```
用户添加成功
```

#### 6.2.2 课程管理接口

**基础路径**：`/courseManagement`

| 接口 | 方法 | 路径 | 功能 |
|------|------|------|------|
| 获取所有课程 | GET | `/courseManagement/getAllCourse` | 查询所有课程 |
| 添加课程 | POST | `/courseManagement/addCourse` | 添加新课程 |
| 更新课程 | PUT | `/courseManagement/updateCourse` | 更新课程信息 |
| 删除课程 | DELETE | `/courseManagement/deleteCourse/{name}` | 删除课程 |

#### 6.2.3 选课管理接口

**基础路径**：`/courseSelect`

| 接口 | 方法 | 路径 | 功能 |
|------|------|------|------|
| 选课 | PUT | `/courseSelect/selectOneCourse` | 学生选课 |
| 退课 | DELETE | `/courseSelect/deleteCourseSelection/{student_id}/{course_id}` | 学生退课 |

#### 6.2.4 成绩管理接口

**基础路径**：`/scoreManagement`

| 接口 | 方法 | 路径 | 功能 |
|------|------|------|------|
| 录入成绩 | POST | `/scoreManagement/addScore` | 录入学生成绩 |
| 查询成绩 | GET | `/scoreManagement/getScore` | 查询学生成绩 |

**请求示例**：
```
POST /scoreManagement/addScore?StudentId=1&CourseId=1&score=85
```

---

## 7. 前端设计

### 7.1 页面结构

```
src/frontPage/
├── 登陆界面.html          # 用户登录页面
├── 注册界面.html          # 用户注册页面
├── 内部表单.html          # 内部功能页面
├── index.css              # 登录页面样式
└── indexx.css             # 注册页面样式
```

### 7.2 前端交互方式

- **注册页面**：使用JavaScript Fetch API发送JSON格式的POST请求
- **登录页面**：使用JavaScript Fetch API发送GET请求（带查询参数）

### 7.3 前端技术特点

- 原生JavaScript实现，无框架依赖
- 使用Fetch API进行HTTP通信
- 支持JSON数据格式
- 响应式设计（通过CSS媒体查询）

---

## 8. 部署说明

### 8.1 环境要求

- **JDK**：17或更高版本
- **MySQL**：8.0或更高版本
- **Maven**：3.6或更高版本
- **操作系统**：Windows/Linux/macOS

### 8.2 数据库配置

1. **创建数据库**：
```sql
CREATE DATABASE student_system CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

2. **配置文件**：`src/main/resources/application.properties`
```properties
spring.datasource.url=jdbc:mysql://localhost:3306/student_system?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=your_password
```

3. **自动建表**：系统启动时会自动创建表（`spring.jpa.hibernate.ddl-auto=update`）

### 8.3 启动步骤

1. **克隆项目**：
```bash
git clone <repository-url>
cd studentGradeManagmentSystem
```

2. **配置数据库**：修改`application.properties`中的数据库连接信息

3. **编译项目**：
```bash
mvn clean compile
```

4. **运行项目**：
```bash
mvn spring-boot:run
```

5. **访问应用**：
- 后端API：`http://localhost:8089`
- 前端页面：直接在浏览器打开HTML文件

### 8.4 打包部署

```bash
# 打包
mvn clean package

# 运行jar包
java -jar target/studentGradeManagmentSystem-0.0.1-SNAPSHOT.jar
```

---

## 9. 开发规范

### 9.1 代码规范

- **命名规范**：
  - 类名：大驼峰（PascalCase）
  - 方法名、变量名：小驼峰（camelCase）
  - 常量：全大写下划线分隔（UPPER_SNAKE_CASE）
  - 包名：全小写

- **注释规范**：
  - 类和方法需要添加JavaDoc注释
  - 复杂逻辑需要添加行内注释

### 9.2 安全规范

- **密码存储**：当前为明文存储，建议使用BCrypt加密
- **SQL注入防护**：使用JPA参数化查询，已自动防护
- **跨域配置**：当前允许所有来源，生产环境应限制特定域名
- **敏感信息**：`application.properties`已加入`.gitignore`，避免泄露

### 9.3 异常处理

- **空值检查**：所有Service层方法都进行了空值检查
- **参数验证**：Controller层对关键参数进行了验证
- **错误提示**：返回明确的错误信息，不返回null

### 9.4 日志规范

- 使用Spring Boot默认日志配置
- SQL语句已配置显示（`spring.jpa.show-sql=true`）

---

## 10. 系统特性

### 10.1 已实现功能

✅ 用户注册、登录、删除   
✅ 课程增删改查      
✅ 学生选课、退课    
✅ 成绩录入、查询  
✅ 跨域支持  
✅ 自动建表  
